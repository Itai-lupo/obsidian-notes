allow the user to see and edit(as namespace root see [[user namespace]]) only the [[cgroups]] that are in that namespace.
that allow for cgroup maniplations on process within [[pid namespaces]] with out system root and it will prevent collision between cgroups with the same name

## see more

man 7 cgroups
man 7 cgroup_namespaces
![[Linux Container Primitives- cgroups, namespaces, and more!]]
![[A Linux sysadmin's introduction to cgroups - Enable Sysadmin]]

